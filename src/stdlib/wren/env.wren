foreign class Env {
    construct new() {}
    foreign static get(key)
    foreign static set(key, value)
    foreign static remove(key)
    foreign static has(key)
    foreign static all()
    foreign static keys()
    foreign static home()
    foreign static user()
    foreign static os()
    foreign static arch()
}
