foreign class Args {
    construct new() {}
    foreign static all()
    foreign static count()
    foreign static get(index)
    foreign static first()
    foreign static last()
    foreign static has(index)
    foreign static slice(start, end)
}
