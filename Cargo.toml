[package]
name = "wrun"
version = "0.4.0"
edition = "2024"
description = "A CLI runner for Wren scripts with system integration"
license = "MIT"

[dependencies]
ruwren = "0.4.10"
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
notify = "6"
